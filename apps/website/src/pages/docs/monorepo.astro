---
import DocsLayout from '../../layouts/DocsLayout.astro';
---

<DocsLayout
  slug="monorepo"
  title="参与仓库开发"
  description="如果你想给脚手架本身贡献代码，可以看这一页。"
>
  <h1>参与仓库开发</h1>
  <p>
    这一页面向<strong>维护脚手架源码</strong>的贡献者。如果你只是想用小工具脚手架做项目，看
    <a href="/docs/getting-started/">快速开始</a> 就够了。
  </p>
  <p>
    本仓库既是脚手架源码，也是 demo 集合。源码在
    <a href="https://github.com/wenyuanw/xhs-minitool" target="_blank" rel="noopener">GitHub</a>。
  </p>

  <h2>在仓库里新建一个 demo 工具</h2>
  <p>克隆仓库后，在根目录运行：</p>
  <pre><code>pnpm install
pnpm create-minitool</code></pre>
  <p>会在 <code>apps/&lt;名称&gt;/</code> 下生成新工具，并自动使用仓库内的共享构建包。</p>

  <h2>本地开发某个工具</h2>
  <pre><code>pnpm --filter &lt;名称&gt; dev
pnpm --filter &lt;名称&gt; build
pnpm --filter &lt;名称&gt; validate</code></pre>

  <h2>更新官网上的 demo 预览</h2>
  <p>先构建全部工具，再构建网站：</p>
  <pre><code>pnpm build
pnpm site:build</code></pre>
  <p>构建时会把各工具的预览页同步到官网的 <code>/preview/&lt;名称&gt;/</code> 路径。</p>

  <h2>开发规范放在哪里</h2>
  <p>
    <code>.agents/skills/</code> 目录里有打包规范和容器能力说明，供 Cursor、Claude Code 等 AI
    工具读取，也方便人工查阅。
  </p>

  <h2>发布到 npm</h2>
  <p>四个工具包会一起发版。完整流程（版本号、CHANGELOG、发布命令）见仓库里的 <a href="https://github.com/wenyuanw/xhs-minitool/blob/main/docs/DEVELOPMENT.md" target="_blank" rel="noopener">docs/DEVELOPMENT.md</a>。</p>
</DocsLayout>
