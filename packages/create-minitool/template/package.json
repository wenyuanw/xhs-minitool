{
  "name": "{{name}}",
  "version": "0.1.0",
  "private": true,
  "description": "{{description}}",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && minitool-pack",
    "preview": "vite preview --port 5174",
    "validate": "minitool-validate xhs-tool --strict"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "xhs-minitool-pack": "{{depVersion}}",
    "xhs-minitool-validate": "{{depVersion}}",
    "xhs-minitool-vite-config": "{{depVersion}}"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
